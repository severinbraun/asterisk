[general]
static=yes
writeprotect=no
clearglobalvars=no

[public]
; 600 = Echo-Test (zur Funktionsprüfung)
exten => 600,1,Answer()
 same => n,Echo()
 same => n,Hangup()

; 610 = einfache Ansage
exten => 610,1,Answer()
 same => n,Playback(demo-congrats)
 same => n,Hangup()

exten => 1002,1,Dial(PJSIP/1002,20)
 same  => n,Hangup()

exten => 2003,1,Dial(PJSIP/2003,20)
 same  => n,Hangup()
