<VirtualHost *:80>
  ServerName localhost
  DocumentRoot /var/www/html

  <Directory "/var/www/html">
    AllowOverride All
    Require all granted
  </Directory>

  ErrorLog ${APACHE_LOG_DIR}/asterisk-error.log
  CustomLog ${APACHE_LOG_DIR}/asterisk-access.log combined
</VirtualHost>
